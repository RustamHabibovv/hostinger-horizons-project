# ============================================
# Hostinger Horizons AI Code Editor - Configuration
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# LLM PROVIDER
# ============================================
# OpenRouter (recommended): https://openrouter.ai/keys
# OpenAI: https://platform.openai.com/api-keys

LLM_API_KEY=your-api-key-here
LLM_BASE_URL=https://openrouter.ai/api/v1

# ============================================
# MODEL SELECTION
# ============================================
# For OpenRouter: provider/model (e.g., anthropic/claude-sonnet-4, google/gemini-2.5-flash)
# For OpenAI: model name (e.g., gpt-4o, gpt-4o-mini)
# 
# Strategy: Use cheaper models for simple tasks, powerful models for code generation

# Simple /api/v1/generate endpoint
MODEL_SIMPLE=anthropic/claude-sonnet-4

# Multi-agent pipeline (/api/v1/agent/generate)
MODEL_INTENT=google/gemini-2.5-flash-lite      # Intent parsing (cheap, fast)
MODEL_PLANNER=google/gemini-2.5-flash-lite     # Execution planning (cheap, fast)
MODEL_EXECUTOR=anthropic/claude-sonnet-4       # Code generation (best quality)

# ReAct agent (/api/v1/react/generate)
MODEL_REACT=anthropic/claude-sonnet-4

# Embeddings for semantic search
MODEL_EMBEDDING=text-embedding-3-small

# ============================================
# OUTPUT FORMAT
# ============================================
# Options: full_content, search_replace, diff
# - full_content: LLM returns complete file content (most reliable, more tokens)
# - search_replace: LLM returns search/replace blocks (balanced)
# - diff: LLM outputs unified diff directly (fewest tokens, less reliable)

OUTPUT_FORMAT=full_content           # Simple endpoint
AGENT_OUTPUT_FORMAT=full_content     # Agent executor

# ============================================
# GENERATION SETTINGS
# ============================================
LLM_TEMPERATURE=0.2
MAX_TOKENS=8192

# ============================================
# AGENT SETTINGS
# ============================================
AGENT_MAX_RETRIES=3              # Max retries on build/validation failure
AGENT_RETRIEVAL_TOP_K=5          # Number of files to retrieve for context
AGENT_VALIDATE_BUILD=true        # Run npm build validation
AGENT_VALIDATION_TIMEOUT=60      # Build timeout in seconds
AGENT_VERBOSE=false              # Include detailed trace in response

# ============================================
# REACT AGENT SETTINGS
# ============================================
REACT_MAX_ITERATIONS=15          # Max reasoning/action cycles

# ============================================
# PROJECT SETTINGS
# ============================================
PROJECTS_BASE_PATH=../sample-react-projects
